#include<stdio.h>
#include<stdlib.h>

// puts gets fosksiyonu gets alma puts yazma!

int main () {
    char bilgi[50];
    printf("bilgi giriniz:");
    gets(bilgi);
    printf("\n\n");
    puts(bilgi);
    return 0;
}
